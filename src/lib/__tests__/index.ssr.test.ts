// import * as Navigators from '../index.ssr';
// import { createBrowserApp } from '@react-navigation/web';
// import { createSwitchNavigator } from 'react-navigation';

describe('Get Naivgator Functions Tests', () => {
	it('it should return createSwitchNavigator by default', () => {
		// expect(
		// 	Navigators.createSwitchNavigator ||
		// 	Navigators.createStackNavigator ||
		// 	Navigators.createMaterialTopTabNavigator ||
		// 	Navigators.createDrawerNavigator ||
		// 	Navigators.createBottomTabNavigator).toBe(createSwitchNavigator);
		// expect(Navigators.createSwitchNavigator).toBe(createSwitchNavigator);
		// expect(Navigators.createStackNavigator).toBeTruthy();
	});
	// it('it should return createContainer', () => {
	// 	expect(WebNavigator.createContainer).toBe(createBrowserApp);
	// });
});