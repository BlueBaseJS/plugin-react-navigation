// import { NavigationActions } from '../NavigationActions';
// import { mount } from 'enzyme';

describe('Navigation tests', () => {
	test('Test', () => {
		expect(2 + 2).toBe(4);
		// const wrapper = mount(
		// 	NavigationActions
		// );
		// const navigationToActionObject = jest.fn();

		// expect(navigationToActionObject).toBeCalled();
	});
});